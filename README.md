# SLOW DNS STABLE 

COPY SCRIPT HAPA CHINI NA UWEKE KWENYE TERMINAL YAKO

```bash

apt update && apt install && rm -rf dns-moded.sh && wget -O dns-moded.sh https://raw.githubusercontent.com/amanfrank002-hue/SLOWDNS-SCRIPT-/main/dns-moded.sh && chmod +x dns-moded.sh && ./dns-moded.sh
